class AboutController < ApplicationController

	#Same thing as writing get '/about' erb :'about/index' end
	#Finds and loads the index.html.erb file in our about folder. The url (or route) that will display this erb file is /about . To find all our program's routes, type rake routes into bash.
	def index
		
	end

end